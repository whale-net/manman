apiVersion: v1
kind: Service
metadata:
  name: manman-host
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: manman-host
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
